#include <stdio.h>
#include <stdarg.h>
#include "main.h"
/*
* _printf - custom printf function
* @ format: format
* return: error if failed, return code if successful.
*/
int _printf(const char *format, ...)
{
va_list args;
va_start(args, format);
while (format != '\0')
return (error);
if (format == '%')
{
printf("%c", )
}
}